import Color = require('tinycolor2')
import {createStyles} from '@material-ui/core'

// the app header at the very top
export const headerHeight = 60
export const menuMargin = 10
export const menuItemSpacing = 2
export const panelBorderRadius = 4
export const statusWindowWidth = 400
// default height of the material-ui tab bar
export const tabBarHeight = 48

export const btnColor = Color('#555555')
export const btnTextColor = btnColor.clone().lighten(50)
export const colors = {
  saffron: Color('#f7c431'),
}

// this is the computed height of the Help menu button, which is generated by
// jQuery UI.
export const jQueryAccordionItemHeight = 27

type TriangleOptions = {
  color?: string
  size?: number
}

// eslint-disable-next-line typescript/explicit-function-return-type
export const triangle = ({color, size}: TriangleOptions) =>
  createStyles({
    right: {
      width: 0,
      height: 0,
      border: '0 solid transparent',
      borderBottomWidth: size || 5,
      borderTopWidth: size || 5,
      borderLeft: `6px solid ${color || 'gray'}`,
    },
  })
