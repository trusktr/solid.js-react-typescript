-
doctype html
html(lang="en")
	head
		meta(charset='utf-8')
		title="Image Detail"
		style.
			#image_list {
				padding: 5px;
			}
		style.
			.image_default {
				border: 1px solid #666;
			}
		style.
			.image_highlighted {
				border: 1px solid #fff;
			}

	body
		#root
			script.
				require(process.cwd() + '/dist/app/annotator-image-lightbox')
		#image_list
